<%= form_for(@grandparent) do |f| %>
  <% if @grandparent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grandparent.errors.count, "error") %> prohibited this grandparent from being saved:</h2>

      <ul>
      <% @grandparent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
